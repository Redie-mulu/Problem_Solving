package march17;

public class Note {
    /*
    int n = 0;
        if(temp.length() > 1){
            for (int i = 0; i < s.length()-1; i++) {
                int num = 0;
//                if((s.charAt(i) == 'I' && s.charAt(i+1) == 'V' || s.charAt(i) == 'I' && s.charAt(i+1) == 'X') ||
//                        (s.charAt(i) == 'X' && s.charAt(i+1) == 'L' || s.charAt(i) == 'X' && s.charAt(i+1) == 'C') ||
//                        (s.charAt(i) == 'C' && s.charAt(i+1) == 'D' || s.charAt(i) == 'C' && s.charAt(i+1) == 'M')){
//                    int n1 = romanNum.get(s.charAt(i));
//                    int n2 = romanNum.get(s.charAt(i+1));
//                    num = n1 * n2;
//                }
                if(romanNum.get("" + s.charAt(i)) < romanNum.get("" + s.charAt(i + 1)) ){
//                    System.out.println(romanNum.get("" + s.charAt(i + 1)));
//                    System.out.println(romanNum.get("" + s.charAt(i )));
                    n = romanNum.get("" + s.charAt(i + 1)) - (romanNum.get("" + s.charAt(i)));
                    i = i+2;
                }

               else {

                    temp = "" + s.charAt(i);
                    int nn = romanNum.get(temp);
                    n += nn;
                }

            }
        }
        else {
            n = romanNum.get(s);
        }
       return n;
    }
     */
}
